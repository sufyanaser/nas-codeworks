# NAS CodeWorks — Updated Context Pack

This package contains the current working context for building the NAS CodeWorks public website.

Recommended local location:

```text
C:\Projects\NAS CodeWorks
```

Recommended structure after extraction:

```text
C:\Projects\NAS CodeWorks
│
├─ docs
│  ├─ 01-strategy
│  ├─ 02-copy
│  ├─ 03-identity
│  └─ 04-build
│
├─ website
└─ README.md
```

The actual website code should be created inside:

```text
C:\Projects\NAS CodeWorks\website
```

The `docs` folder is context/source-of-truth. Do not mix context files with website source files.

## Start Here

1. Read `docs/00-authority/01_Project_Constitution_v2.md` — the highest authority (consolidates positioning, services, pricing, CTA, identity, and the Experience Architecture).
2. Read `docs/00-authority/02_Document_Map.md` and `docs/00-authority/03_Project_Status.md`.
3. Read `docs/00_START_HERE.md` and the files in `docs/01-strategy`.
4. Read the approved copy in `docs/02-copy`.
5. Read the identity files in `docs/03-identity`.
6. Use `docs/04-build/01_Website_UI_Build_Brief.md` (updated to Constitution v2) as the build direction. The older `02_Website_Build_Execution_Prompt.md` is archived in `docs/archived/` and is not authoritative.

## Current phase

Execution inside the selected build tool.

## Critical rule

Do not redesign the strategy. Build the website according to the approved context.
